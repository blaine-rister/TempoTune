<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.ActionBar.TitleTextStyle" parent="@style/TextAppearance.AppCompat.Title">
        <item name="android:textSize">20sp</item> <!-- Default for portrait is 20sp and for landscape 14sp-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.instrumentSpinnerItemStyle"
        parent="@android:style/Widget.TextView.SpinnerItem">
        <item name="android:textSize">@dimen/instrument_text_size</item>
        <item name="android:paddingLeft">@dimen/instrument_text_horizontal_padding</item>
    </style>

    <style name="AppTheme.pitchSpinnerItemStyle"
        parent="@android:style/Widget.TextView.SpinnerItem">
        <item name="android:textSize">@dimen/pitch_text_size</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">@dimen/pitch_vertical_padding</item>
        <item name="android:paddingBottom">@dimen/pitch_vertical_padding</item>
        <item name="android:paddingLeft">@dimen/pitch_horizontal_padding</item>
        <item name="android:paddingRight">@dimen/pitch_horizontal_padding</item>
    </style>

    <style name="AppTheme.sectionTitleTextStyle"
        parent="TextAppearance.AppCompat.Medium">
        <item name="android:padding">@dimen/section_title_padding</item>
        <item name="android:gravity">start</item>
    </style>

</resources>
